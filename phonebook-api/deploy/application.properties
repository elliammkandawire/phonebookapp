#This is port for api
server.port=8010

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

# Enable spring data repos
spring.data.jpa.repositories.enabled=true

# Replace with your connection string. Replace the port if your mysql is running on a different port to this one
spring.datasource.url=jdbc:mysql://localhost:3306/phonebook

# Replace with your database credentials
spring.datasource.username=root
spring.datasource.password=

spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

#username and password for creation of bearer token
email=mkandawireelliam@yahoo.com
password=Elliam2022@